package com.bee.launch

import android.content.Context
import com.bee.core.delegate.IApplicationDelegate

/**
 *@Description:
 *
 */
class LaunchApplicationDelegate : IApplicationDelegate {
    override fun onCreate(context: Context) {
    }
}
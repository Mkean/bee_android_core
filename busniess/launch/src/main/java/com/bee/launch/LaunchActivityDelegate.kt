package com.bee.launch

import android.content.Context
import com.bee.core.delegate.IActivityDelegate

/**
 *@Description:
 *
 */
class LaunchActivityDelegate : IActivityDelegate {
    override fun onCreate(context: Context) {
    }
}